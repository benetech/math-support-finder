-unless object.notes.blank?
  %h2 Notes
  = to_html(object.notes)
%h3
  Read more about 
  = parent

-if !current_user
  - setups = object.setups.collect{|s| s if [2, 3].include? s.workflow_status_id }.compact
-else
  - setups = object.setups
- if setups.length > 0
  = render "setups_table", setups: setups
